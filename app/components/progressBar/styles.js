import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container : {
        bottom : 70,
        left : 0,
    },
    progress : {
        borderRadius : 20,
        marginHorizontal : 10,
    }
})

export default styles;